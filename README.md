# 🏦 Система управления банковскими картами

Комплексная система управления банковскими картами, построенная на Spring Boot с JWT аутентификацией, ролевым контролем доступа и безопасными переводами между картами.

## 🎯 Функциональность

### 👨‍💼 Администратор
- ✅ Создание, блокировка, активация и удаление карт
- ✅ Управление пользователями и просмотр всех данных системы
- ✅ Обработка запросов на блокировку карт
- ✅ Просмотр подробной статистики переводов
- ✅ Полные административные возможности
- ✅ Автоматическое истечение карт (планировщик)

### 👤 Пользователь
- ✅ Просмотр собственных карт с поиском и пагинацией
- ✅ Запрос блокировки карты с указанием причины
- ✅ Переводы между картами (включая карты других пользователей)
- ✅ Получение CVV кода для верификации переводов
- ✅ Просмотр балансов карт и истории переводов
- ✅ Безопасное управление профилем

## 🔒 Функции безопасности

- **JWT Аутентификация** - Безопасная токен-основанная аутентификация
- **Ролевой контроль доступа** - Роли ADMIN и USER
- **Шифрование данных** - Номера карт и CVV зашифрованы с помощью AES
- **Хеширование паролей** - BCrypt для безопасного хранения паролей
- **Маскировка карт** - Конфиденциальные данные замаскированы в ответах
- **Контроль доступа** - Пользователи могут получить доступ только к своим данным

## 🚀 Быстрый старт

### Требования
- Java 17+
- Maven 3.6+
- Docker & Docker Compose

### 🐳 Рекомендуемый: Гибридная разработка
```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd bank_rest

# 2. Запустить PostgreSQL в Docker
docker-compose up postgres -d

# 3. Запустить приложение локально
mvn spring-boot:run "-Dspring.profiles.active=local"

# 4. Доступ к приложению
# API: http://localhost:8080/api/v1
# Swagger: http://localhost:8080/api/v1/swagger-ui.html
```

### 🐳 Полная настройка Docker
```bash
# Запустить все сервисы
docker-compose up -d

# Просмотр логов
docker-compose logs -f app
```

## 🔑 Учетные данные по умолчанию

### Системный администратор
- **Email**: admin@mail.ru
- **Пароль**: password
- **Роль**: ADMIN

### Тестовые пользователи
- **Иван**: ivan@gmail.com / Ivan1234 (ROLE_USER)
- **Петр**: petr@mail.ru / Petr1234 (ROLE_USER)

## 📚 Документация API

### Интерактивная документация
- **Swagger UI**: http://localhost:8080/api/v1/swagger-ui.html
- **OpenAPI JSON**: http://localhost:8080/api/v1/api-docs
- **Проверка состояния**: http://localhost:8080/api/v1/actuator/health

### Быстрый тест API
```bash
# 1. Войти в систему и получить токен
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@mail.ru","password":"password"}'

# 2. Использовать токен для доступа к защищенным эндпоинтам
curl -X GET http://localhost:8080/api/v1/cards \
  -H "Authorization: Bearer ВАШ_ТОКЕН_ЗДЕСЬ"
```

## 🏗️ Архитектура

### Технологический стек
- **Backend**: Java 17, Spring Boot 3.2, Spring Security
- **База данных**: PostgreSQL с миграциями Liquibase
- **Аутентификация**: JWT токены с механизмом обновления
- **Документация**: OpenAPI 3.0 с Swagger UI
- **Тестирование**: JUnit 5, Mockito, 14 тестовых файлов
- **Контейнеризация**: Docker & Docker Compose

### Структура проекта
```
src/
├── main/java/com/example/bankcards/
│   ├── config/          # Классы конфигурации
│   ├── controller/      # REST контроллеры
│   ├── dto/            # Объекты передачи данных
│   ├── entity/         # JPA сущности
│   ├── exception/      # Пользовательские исключения
│   ├── repository/     # Репозитории данных
│   ├── security/       # Компоненты безопасности
│   ├── service/        # Бизнес-логика
│   ├── scheduler/      # Запланированные задачи (истечение карт)
│   └── util/           # Утилитарные классы
├── main/resources/
│   ├── db/migration/   # Миграции Liquibase
│   └── application*.yml # Файлы конфигурации
└── test/               # Модульные и интеграционные тесты
```

## 🗄️ База данных

### Автоматическая настройка
- **Миграции Liquibase** автоматически создают все таблицы
- **Тестовые данные** вставляются при первом запуске
- **Зашифрованное хранение** для конфиденциальных данных карт
- **PostgreSQL** как основная база данных

### Создаваемые таблицы
- `users` - Учетные записи и профили пользователей
- `cards` - Банковские карты с зашифрованными данными
- `transfers` - Денежные переводы между картами
- `card_block_requests` - Запросы на блокировку карт
- `databasechangelog` - История миграций Liquibase

## 🧪 Тестирование

### Запуск тестов
```bash
# Модульные тесты (с мокированными зависимостями)
mvn test

# Интеграционные тесты с PostgreSQL
docker-compose -f docker-compose.yml -f docker-compose.test.yml up -d
mvn test "-Dspring.profiles.active=test"
```

### Покрытие тестами
- **14 тестовых файлов** покрывающих основную функциональность
- **Модульные тесты** для сервисов и утилит
- **Интеграционные тесты** для контроллеров
- **Тесты безопасности** для аутентификации и авторизации

## 🔧 Разработка

### Профили окружения
- **local** - Локальная разработка с PostgreSQL в Docker
- **test** - Тестовое окружение
- **prod** - Производственная конфигурация

### Конфигурация
```yaml
# Ключевая конфигурация в application.yml
server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  profiles:
    active: local
  
jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000  # 24 часа
  refresh-expiration: 604800000  # 7 дней
```

## 📖 Документация

### Доступные руководства
- **[INSTRUCTION.md](docs/INSTRUCTION.md)** - Полное руководство по настройке и развертыванию
- **[LIQUIBASE.md](docs/LIQUIBASE.md)** - Управление миграциями базы данных
- **[Security README](src/main/java/com/example/bankcards/security/README_Security.md)** - Архитектура безопасности

### Примеры API
Подробные примеры API с форматами запросов/ответов, обработкой ошибок и потоками аутентификации доступны в файлах документации.

## 🚀 Развертывание

### Производственное развертывание
```bash
# Производственное окружение
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

### Переменные окружения
```bash
# Необходимо для производства
JWT_SECRET=ваш_производственный_секретный_ключ_минимум_32_символа
DB_PASSWORD=ваш_безопасный_пароль_базы_данных
CORS_ALLOWED_ORIGINS=https://yourdomain.com
ENCRYPTION_KEY=ваш_ключ_шифрования_32_символа
```

## 🤝 Участие в разработке

1. Следуйте существующему стилю кода и архитектуре
2. Добавляйте тесты для новой функциональности
3. Обновляйте документацию по мере необходимости
4. Убедитесь, что все тесты проходят перед отправкой

## 📄 Лицензия

Этот проект разработан как техническая демонстрация современной архитектуры приложения Spring Boot с комплексными практиками безопасности и тестирования.